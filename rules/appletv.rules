// ------------------------------
// Rules to control the HK AVR
// ------------------------------


rule "Power Apple TV on/off"
  when 
    Item SW_APLTV received command
  then
    if (receivedCommand == ON) {
        HARMONY_APLTV.sendCommand("Play")
    } else {
        HARMONY_APLTV.sendCommand("Stop")
    }
  end
