rule "Hue_Motion_Flur"
    when
    	Item Hue_Motion_Flur changed
    then
        logInfo("motion.rules", "Motion detected: Flur")
        if((Hue_Motion_Flur.state == ON) && (SWITCH_ALARM_SYSTEM.state == ON)){
            logInfo("motion.rules", "Alarm triggered")
            sendBroadcastNotification("Alarm triggered! - Flur")
        } else {
            logInfo("motion.rules", "Alarm not triggered")
        }
end

rule "Hue_Temp_Kueche"
    when
    	Item Hue_Temp_Kueche changed
    then
        logInfo("motion.rules", "Motion detected: Kueche")
        if((Hue_Temp_Kueche.state == ON) && (SWITCH_ALARM_SYSTEM.state == ON)){
            logInfo("motion.rules", "Alarm triggered")
            sendBroadcastNotification("Alarm triggered! - Küche")
        } else {
            logInfo("motion.rules", "Alarm not triggered")
        }
end