// ------------------------------
// Rules to control the TV
// ------------------------------


rule "Power TV on/off"
  when 
    Item SW_LGTV received command
  then
    if (receivedCommand == ON) {
        HarmonyTV.sendCommand("PowerOn")
    } else {
        HarmonyTV.sendCommand("PowerOff")
    }
  end
